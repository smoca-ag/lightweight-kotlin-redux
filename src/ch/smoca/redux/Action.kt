package ch.smoca.redux

/**
 * Represents an action that can be dispatched to the store.
 *
 * Actions are typically implemented as sealed classes or data classes. They encapsulate an event or intent
 * that may result in a state change.
 */
interface Action
